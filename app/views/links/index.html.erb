<h1>Welcome</h1>
<div class="user-info">
  <%= link_to "Sign out", logout_path, method: :delete %>
</div>

<div id="new-link">
  <%= form_for @link, url: "#" do |f| %>
    <%=  f.label :url %>
    <%=  f.text_field :url %>
    <%=  f.label :title %>
    <%=  f.text_field :title %>
    <%= f.submit "Save" %>
  <% end %>
</div>

<table id="all-links">
  <thead>
    <tr>
      <th>Title</th>
      <th>URL</th>
      <th>Read</th>
    </tr>
  </thead>
  <tbody class="links">
    <% @links.each do |link| %>
      <tr id="link-<%= link.id %>">
        <td><%= link.title %></td>
        <td><%= link.url %></td>
        <td data-link-id="<%= link.id %>" class="read-status"><%= link.read %><button class="mark-as-read">Mark as Read</button></td>
        <td><%= link_to "Edit", edit_link_path(link.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
